<b>${event.message}</b>

${if event.timerange_start}
🕒 Period: ${event.timerange_start} - ${event.timerange_end}
${endif}

${if event.streams}
📊 Streams: ${event.streams}
${endif}

${if backlog}
📝 Details:
<code>${foreach backlog msg}${msg.message}
${endfor}</code>
${endif}
